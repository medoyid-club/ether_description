# Приклад: робочий файл — `stream_key.txt` поруч із проєктом (у .gitignore). Можете тримати кілька ключів —
# один Stream key ОДИМ РЯДКОМ, пріоритет згори донизу (спочатку пробує перший ключ, потім другий тощо).
#
# Коментарі починаються з # й ігноруються (плейсхолдер DEFAULT теж ігнорується).
#
# Навіщо liveStreams.list: у YouTube API для bind потрібен `liveStream.id`. Текст ключа сам по собі `insert`не замінює —
# ми лише робимо необов'язкову спробу знайти вже створений ваш потік з таким `ingestionInfo.streamName`. Якщо Google поверне
# помилку на list або ключів серед потоків немає — бот створює новий liveStream і нову пару ingestion/key у папці сесії.
#
DEFAULT
